# StockAnalysis
crawlers and exploratory analysis for taiwan stock market

## Layout
- `apps/`: runnable scripts (crawlers, ETL, services)
- `src/stockanalysis/`: shared library + analysis modules
- `assets/`: static assets/models
- `outputs/`: generated artifacts
- `data/`: local datasets
