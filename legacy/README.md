# Legacy

The duplicated prepare/trigger service source trees were removed during the
Phase 2 cleanup. Git history remains the recovery source for those files.

The active GCP deployment flow is defined in `deployment/`; do not add new
runtime implementations under `legacy/`.
