# Legacy

This directory keeps code that is no longer the primary operational path but is retained for reference.

Current examples:

- `legacy/apps/twse/prepare-service`
- `legacy/apps/twse/trigger-service`
- `legacy/apps/tpex/prepare-service`
- `legacy/apps/tpex/trigger-service`

The active GCP deployment flow is defined in `deployment/`.
